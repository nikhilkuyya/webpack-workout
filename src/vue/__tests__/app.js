import Vue from "vue"
import App from "../App.vue"
// test("test data of App Component", () => {
//   const { rootId, framework } = App.data()
//   expect(rootId).toBe("app")
//   expect(framework).toBe("Vue")
// })

test("Vue App renders", () => {
  console.log(App)
  // expect(vm.$el.textContent).toBe("Vue")
})
